{
  "name": "tax-tracker",
  "version": "1.0.0",
  "description": "Fatura Kayıt ve KDV Takip Uygulaması",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "dev": "concurrently \"cross-env BROWSER=none npm run react-start\" \"wait-on http://localhost:3000 && electron .\"",
    "react-start": "react-scripts start",
    "react-build": "react-scripts build",
    "postbuild": "node scripts/copy-files.js",
    "clean": "node scripts/clean.js",
    "prebuild": "npm run clean",
    "build": "npm run react-build && npm run postbuild && electron-builder --config electron-builder.yml",
    "pack": "electron-builder --dir --config electron-builder.yml",
    "dist": "npm run clean && npm run react-build && npm run postbuild && electron-builder --config electron-builder.yml"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@testing-library/jest-dom": "^5.16.5",
    "@testing-library/react": "^13.4.0",
    "@testing-library/user-event": "^13.5.0",
    "concurrently": "^7.6.0",
    "cross-env": "^7.0.3",
    "electron": "^22.0.0",
    "electron-builder": "^23.6.0",
    "wait-on": "^7.0.1"
  },
  "dependencies": {
    "@ant-design/icons": "^5.0.1",
    "antd": "^5.3.0",
    "chart.js": "^4.2.1",
    "dayjs": "^1.11.7",
    "electron-is-dev": "^2.0.0",
    "react": "^18.2.0",
    "react-chartjs-2": "^5.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.8.2",
    "react-scripts": "5.0.1"
  },
  "browserslist": {
    "production": [
      "last 1 electron version"
    ],
    "development": [
      "last 1 electron version"
    ]
  },
  "homepage": "./"
}
